# 📋 湖北经济学院零星报修系统 - 完整需求对比与实施方案

**文档版本**: v1.0  
**分析日期**: 2026-09-06  
**参考文档**: 湖北经济学院零星报修系统用户需求说明书 v1.1  
**需求总数**: 16条功能需求（P0: 14条，P1: 2条）

---

## 📊 一、用户需求完整理解

### 1.1 系统五大业务阶段

#### Phase 01: 报修阶段 (2条需求)
- **REQ-01**: 报修单填报与提交
- **REQ-02**: 副主任审核报修单

#### Phase 02: 预算阶段 (3条需求)
- **REQ-03**: 乙方接单与预算编制
- **REQ-04**: 副主任预算审核

#### Phase 03: 立项阶段 (4条需求) ⭐ 核心
- **REQ-05**: 发起立项审批
- **REQ-06**: 立项单一级审核（部门主管）
- **REQ-07**: 立项单二级审核（分管领导）
- **REQ-08**: 立项单终审（一把手）
- **关键机制**: 三级逐级审批 + 逐级驳回

#### Phase 04: 签证阶段 (3条需求)
- **REQ-09**: 施工记录与签证单上传
- **REQ-10**: 签证单内容审核（副主任）
- **REQ-11**: 签证单格式审核（资料员）
- **关键机制**: 双路并行审核

#### Phase 05: 验收阶段 (4条需求)
- **REQ-12**: 验收发起与并行审核
- **REQ-13**: 监察人员现场验收
- **REQ-14**: 财务人员费用确认
- **REQ-15**: 结案归档
- **关键机制**: 双路并行审核 + 自动生成结案单

### 1.2 核心业务规则

#### 🔴 逐级驳回机制（立项阶段特有）
```
驳回路径（不越级）:
一把手驳回    → 退回分管领导（重新评估）
分管领导驳回  → 退回部门主管（重新评估）
部门主管驳回  → 退回副主任/主管（重新发起）

每一级都有决策权：
- 可以修改后向上提交
- 可以继续向下驳回
```

#### 🔵 双路并行审核（签证、验收阶段）
```
签证阶段:
副主任（内容审核） ─┐
                    ├─ 都通过 → 签证完成
资料员（格式审核） ─┘

验收阶段:
监察人员（现场确认）─┐
                    ├─ 都通过 → 验收完成
财务人员（费用确认）─┘

规则: 任何一路驳回，立即退回乙方
```

#### 🟢 角色与权限
```
内部角色（甲方）:
- 报修人: 创建报修单
- 副主任/主管: 审核报修、预算、内容，发起立项
- 部门主管: 立项一级审核
- 分管领导: 立项二级审核
- 一把手: 立项终审（意见强制必填）
- 资料员: 格式审核
- 监察人员: 现场验收
- 财务人员: 费用确认

外部角色（乙方）:
- 乙方维修人员: 编制预算、上传签证、发起验收
```

### 1.3 非功能性需求

1. **移动端适配** (P1)
   - 乙方上传功能优先移动端
   - 监察人员现场确认需移动端
   - 4G网络下响应≤3秒

2. **消息通知** (P1)
   - 所有流转1分钟内推送
   - 关键节点支持短信/企微
   - 超时3天自动催办，超7天升级

3. **操作留痕** (P0)
   - 操作人工号+姓名+时间戳+IP
   - 审计日志保留10年
   - 结案后不可编辑

4. **权限管理** (P0)
   - 基于RBAC严格控制
   - 乙方只看自己的工单
   - 管理员可配置参数

5. **统计报表** (P1)
   - 工单数量、审批时长、驳回率
   - 部门频次、乙方金额统计
   - 支持导出Excel/PDF

6. **性能要求** (P0)
   - 可用性≥99.5%
   - PC端≤2秒，移动端≤3秒
   - 并发≥200用户

---

## 📊 二、当前系统 vs 用户需求对比

### 2.1 完整对比矩阵

| 需求编号 | 功能需求 | 优先级 | 当前状态 | 完成度 | 差距说明 |
|---------|---------|--------|---------|--------|---------|
| **Phase 01: 报修** |
| REQ-01 | 报修单填报 | P0 | ✅ 已实现 | 90% | 缺少位置字段、部门字段 |
| REQ-02 | 副主任审核 | P0 | ❌ 缺失 | 0% | 无审核流程 |
| **Phase 02: 预算** |
| REQ-03 | 乙方预算编制 | P0 | ❌ 缺失 | 0% | 无乙方角色、无预算单 |
| REQ-04 | 预算审核 | P0 | ❌ 缺失 | 0% | 无审核流程 |
| **Phase 03: 立项** |
| REQ-05 | 发起立项 | P0 | ❌ 缺失 | 0% | 无立项功能 |
| REQ-06 | 一级审核 | P0 | ❌ 缺失 | 0% | 无多级审批 |
| REQ-07 | 二级审核 | P0 | ❌ 缺失 | 0% | 无多级审批 |
| REQ-08 | 终审 | P0 | ❌ 缺失 | 0% | 无多级审批 |
| **Phase 04: 签证** |
| REQ-09 | 签证单上传 | P0 | ⚠️ 部分 | 40% | 有附件上传，但无签证流程 |
| REQ-10 | 内容审核 | P0 | ❌ 缺失 | 0% | 无双路并行审核 |
| REQ-11 | 格式审核 | P0 | ❌ 缺失 | 0% | 无双路并行审核 |
| **Phase 05: 验收** |
| REQ-12 | 验收发起 | P0 | ⚠️ 部分 | 30% | 有评价，但无正式验收 |
| REQ-13 | 现场验收 | P0 | ❌ 缺失 | 0% | 无双路并行审核 |
| REQ-14 | 费用确认 | P0 | ❌ 缺失 | 0% | 无财务确认 |
| REQ-15 | 结案归档 | P0 | ⚠️ 部分 | 30% | 有关闭，但无结案单 |
| **跨阶段功能** |
| REQ-16 | 通知与催办 | P1 | ⚠️ 部分 | 60% | 有通知，但无催办 |

**图例**: ✅ 已完整实现 | ⚠️ 部分实现 | ❌ 完全缺失

### 2.2 完成度统计

| 类别 | 数量 | 占比 |
|-----|------|------|
| 已完整实现 (90%+) | 1条 | 6.25% |
| 部分实现 (30-60%) | 4条 | 25% |
| 完全缺失 (0%) | 11条 | 68.75% |

**总体完成度**: **约15-20%**

---

## 🎯 三、核心差距分析

### 3.1 缺失的核心功能

#### 🔴 优先级P0 - 必须实现（核心阻断）

**1. 多级审批流程引擎** ❌
- **影响**: 无法实现立项三级审批
- **涉及需求**: REQ-05/06/07/08
- **复杂度**: 高
- **估算工时**: 40小时

**2. 逐级驳回机制** ❌
- **影响**: 无法实现审批不通过的逐级退回
- **涉及需求**: REQ-06/07/08
- **复杂度**: 中高
- **估算工时**: 24小时

**3. 双路并行审核** ❌
- **影响**: 无法实现签证和验收的并行审核
- **涉及需求**: REQ-10/11/13/14
- **复杂度**: 中
- **估算工时**: 20小时

**4. 预算管理** ❌
- **影响**: 无预算编制和审核环节
- **涉及需求**: REQ-03/04
- **复杂度**: 中
- **估算工时**: 24小时

**5. 立项管理** ❌
- **影响**: 无独立立项流程
- **涉及需求**: REQ-05/06/07/08
- **复杂度**: 高
- **估算工时**: 32小时

**6. 签证管理** ❌
- **影响**: 无正式签证审核流程
- **涉及需求**: REQ-09/10/11
- **复杂度**: 中
- **估算工时**: 24小时

**7. 验收管理** ❌
- **影响**: 无正式验收确认流程
- **涉及需求**: REQ-12/13/14/15
- **复杂度**: 中高
- **估算工时**: 28小时

**8. 外部角色（乙方）** ❌
- **影响**: 无法管理外部协作方
- **涉及需求**: REQ-03/09/12
- **复杂度**: 中
- **估算工时**: 16小时

**9. 角色细分** ⚠️
- **影响**: 当前角色不够细分
- **涉及需求**: 所有
- **复杂度**: 中
- **估算工时**: 20小时

**10. 结案单生成** ❌
- **影响**: 无自动生成结案归档文件
- **涉及需求**: REQ-15
- **复杂度**: 低
- **估算工时**: 12小时

#### 🟡 优先级P1 - 重要（显著影响体验）

**11. 催办机制** ❌
- **影响**: 无超时自动催办
- **涉及需求**: REQ-16
- **复杂度**: 低
- **估算工时**: 8小时

**12. 移动端优化** ⚠️
- **影响**: 乙方和监察人员操作不便
- **涉及需求**: NFR-移动端
- **复杂度**: 中
- **估算工时**: 40小时

### 3.2 需要调整的现有功能

#### 🔄 数据库Schema调整

**1. Ticket表扩展**
```prisma
model Ticket {
  // 新增字段
  location         String?        // 报修位置
  department       String?        // 报修部门
  currentPhase     Phase          // 当前阶段
  currentStep      Int?           // 当前步骤
  
  // 关联关系
  approvalFlow     ApprovalFlow?  // 审批流程
  budget           Budget?        // 预算单
  project          Project?       // 立项单
  certificate      Certificate?   // 签证单
  acceptance       Acceptance?    // 验收记录
  settlement       Settlement?    // 结案单
}

enum Phase {
  REPAIR      // 报修阶段
  BUDGET      // 预算阶段
  PROJECT     // 立项阶段
  CERTIFICATE // 签证阶段
  ACCEPTANCE  // 验收阶段
  COMPLETED   // 已完成
}
```

**2. User表扩展**
```prisma
model User {
  // 新增字段
  employeeId       String         // 工号
  isExternal       Boolean        // 是否外部用户（乙方）
  organization     String?        // 所属单位（乙方）
  
  // 角色细分
  role             UserRole
}

enum UserRole {
  // 内部角色
  REPORTER         // 报修人
  VICE_DIRECTOR    // 副主任/主管
  DEPT_MANAGER     // 部门主管
  VICE_LEADER      // 分管领导
  TOP_LEADER       // 一把手
  DATA_ADMIN       // 资料员
  INSPECTOR        // 监察人员
  FINANCE          // 财务人员
  
  // 外部角色
  CONTRACTOR       // 乙方维修人员
  
  // 系统角色
  ADMIN            // 系统管理员
}
```

**3. 新增核心表**

```prisma
// 审批流程表
model ApprovalFlow {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  type            ApprovalType   // REPAIR/BUDGET/PROJECT
  currentStep     Int            @default(1)
  totalSteps      Int
  status          FlowStatus     @default(PENDING)
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
  steps           ApprovalStep[]
}

// 审批步骤表
model ApprovalStep {
  id              BigInt         @id @default(autoincrement())
  flowId          BigInt
  stepNumber      Int            // 1,2,3...
  stepName        String         // "一级审核"/"二级审核"
  approverRole    UserRole       // 审批角色
  approverId      BigInt?        // 审批人ID
  status          StepStatus     @default(PENDING)
  comment         String?        @db.Text // 审批意见
  approvedAt      DateTime?
  rejectedAt      DateTime?
  
  flow            ApprovalFlow   @relation(fields: [flowId], references: [id])
  approver        User?          @relation(fields: [approverId], references: [id])
}

enum ApprovalType {
  REPAIR      // 报修审核
  BUDGET      // 预算审核
  PROJECT     // 立项审批（三级）
}

enum FlowStatus {
  PENDING     // 进行中
  APPROVED    // 已通过
  REJECTED    // 已驳回
  CANCELLED   // 已取消
}

enum StepStatus {
  PENDING     // 待审批
  APPROVED    // 已通过
  REJECTED    // 已驳回
  SKIPPED     // 已跳过
}

// 预算单表
model Budget {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  amount          Decimal        @db.Decimal(10, 2)
  description     String         @db.Text
  attachments     String[]       // 预算文件URL
  status          BudgetStatus   @default(DRAFT)
  createdBy       BigInt         // 乙方编制人
  createdAt       DateTime       @default(now())
  approvedBy      BigInt?        // 副主任审核人
  approvedAt      DateTime?
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
  creator         User           @relation("BudgetCreator", fields: [createdBy], references: [id])
}

enum BudgetStatus {
  DRAFT           // 草稿
  SUBMITTED       // 已提交
  APPROVED        // 已批准
  REJECTED        // 已驳回
}

// 立项单表
model Project {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  title           String         @db.VarChar(200)
  plannedStartDate DateTime      // 计划开工日期
  plannedDuration Int            // 计划工期(天)
  contractorName  String         // 乙方名称
  description     String?        @db.Text
  status          ProjectStatus  @default(DRAFT)
  createdBy       BigInt         // 副主任发起
  createdAt       DateTime       @default(now())
  approvedAt      DateTime?
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
  creator         User           @relation(fields: [createdBy], references: [id])
  approvalFlow    ApprovalFlow?
}

enum ProjectStatus {
  DRAFT           // 草稿
  PENDING         // 待审批
  APPROVED        // 已批准
  REJECTED        // 已驳回
  IN_PROGRESS     // 施工中
  COMPLETED       // 已完成
}

// 签证单表
model Certificate {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  photos          String[]       // 施工照片URL
  document        String         // 签证单文件URL
  actualFinishDate DateTime      // 实际完工日期
  workDescription String         @db.Text // 实际工程量说明
  status          CertStatus     @default(PENDING)
  createdBy       BigInt         // 乙方上传人
  createdAt       DateTime       @default(now())
  
  // 双路审核
  contentStatus   ReviewStatus   @default(PENDING)
  contentReviewer BigInt?        // 副主任
  contentComment  String?
  contentReviewedAt DateTime?
  
  formatStatus    ReviewStatus   @default(PENDING)
  formatReviewer  BigInt?        // 资料员
  formatComment   String?
  formatReviewedAt DateTime?
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
  creator         User           @relation("CertCreator", fields: [createdBy], references: [id])
}

enum CertStatus {
  PENDING         // 待审核
  APPROVED        // 已通过
  REJECTED        // 已驳回
}

enum ReviewStatus {
  PENDING         // 待审核
  APPROVED        // 已通过
  REJECTED        // 已驳回
}

// 验收表
model Acceptance {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  status          AcceptStatus   @default(PENDING)
  initiatedBy     BigInt         // 乙方发起人
  initiatedAt     DateTime       @default(now())
  
  // 双路审核
  inspectionStatus ReviewStatus  @default(PENDING)
  inspector       BigInt?        // 监察人员
  inspectionPhotos String[]      // 验收照片
  inspectionComment String?
  inspectionDate  DateTime?
  
  financeStatus   ReviewStatus   @default(PENDING)
  financeOfficer  BigInt?        // 财务人员
  actualAmount    Decimal?       @db.Decimal(10, 2)
  financeComment  String?
  financeDate     DateTime?
  
  completedAt     DateTime?
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
  initiator       User           @relation("AcceptInitiator", fields: [initiatedBy], references: [id])
}

enum AcceptStatus {
  PENDING         // 待验收
  IN_PROGRESS     // 验收中
  APPROVED        // 验收通过
  REJECTED        // 验收不通过
}

// 结案单表
model Settlement {
  id              BigInt         @id @default(autoincrement())
  ticketId        BigInt         @unique
  documentUrl     String         // PDF文件URL
  content         Json           // 结案单完整内容
  createdAt       DateTime       @default(now())
  
  ticket          Ticket         @relation(fields: [ticketId], references: [id])
}
```

#### 🔄 API调整

**现有API需要扩展**:
1. `POST /tickets` - 添加location、department字段
2. `PATCH /tickets/:id` - 添加阶段和步骤更新
3. `GET /tickets` - 添加按阶段筛选

**新增API端点**:
```typescript
// 审批流程
POST   /tickets/:id/approve        // 审批通过
POST   /tickets/:id/reject         // 审批驳回
GET    /tickets/:id/approval-flow  // 查看审批流程

// 预算管理
POST   /tickets/:id/budget         // 提交预算
PATCH  /budgets/:id                // 更新预算
POST   /budgets/:id/approve        // 审核预算

// 立项管理
POST   /tickets/:id/project        // 发起立项
GET    /projects/:id               // 查看立项
POST   /projects/:id/approve       // 审批立项（三级）
POST   /projects/:id/reject        // 驳回立项（逐级）

// 签证管理
POST   /tickets/:id/certificate    // 上传签证
POST   /certificates/:id/content-review  // 内容审核
POST   /certificates/:id/format-review   // 格式审核

// 验收管理
POST   /tickets/:id/acceptance     // 发起验收
POST   /acceptances/:id/inspection // 现场验收
POST   /acceptances/:id/finance    // 财务确认
GET    /acceptances/:id/settlement // 查看结案单

// 通知催办
GET    /tickets/overdue            // 超时工单列表
POST   /tickets/:id/remind         // 手动催办
```

---

## 🚀 四、完整实施方案

### 4.1 方案A: 完整实现（推荐正式上线）

**目标**: 100%满足用户需求文档  
**工期**: 20-25个工作日  
**团队**: 2-3名开发人员

#### 第一阶段: 核心审批流程（7天）
**目标**: 实现多级审批和逐级驳回

1. **数据库设计** (1天)
   - 设计ApprovalFlow、ApprovalStep表
   - 设计Budget、Project表
   - 扩展User表（角色细分）
   - 扩展Ticket表（阶段字段）

2. **审批引擎开发** (3天)
   - 审批流程创建
   - 审批节点流转
   - 逐级驳回逻辑
   - 审批历史记录

3. **立项功能开发** (2天)
   - 发起立项
   - 三级审批（部门主管→分管领导→一把手）
   - 逐级驳回
   - 立项通知

4. **测试** (1天)
   - 审批流转测试
   - 驳回逻辑测试
   - 边界情况测试

#### 第二阶段: 预算与签证（5天）

1. **预算管理** (2天)
   - 乙方角色创建
   - 预算单编制
   - 预算审核
   - 预算驳回

2. **签证管理** (2天)
   - 签证单上传
   - 双路并行审核（内容+格式）
   - 并行审核逻辑
   - 签证驳回

3. **测试** (1天)
   - 预算流程测试
   - 签证并行审核测试

#### 第三阶段: 验收与结案（4天）

1. **验收管理** (2天)
   - 验收发起
   - 双路并行审核（监察+财务）
   - 现场验收（移动端）
   - 费用确认

2. **结案归档** (1天)
   - 自动生成结案单
   - PDF导出
   - 归档逻辑

3. **测试** (1天)
   - 验收流程测试
   - 结案单生成测试

#### 第四阶段: 报修审核与完善（4天）

1. **报修审核** (1天)
   - 副主任审核报修
   - 报修驳回

2. **通知催办** (1天)
   - 超时检测
   - 自动催办
   - 升级通知

3. **移动端优化** (2天)
   - 乙方上传功能（移动端）
   - 监察现场验收（移动端）
   - 响应式优化

#### 第五阶段: 前端界面开发（7天）

1. **审批界面** (2天)
   - 立项审批页面
   - 审批历史展示
   - 驳回操作界面

2. **预算与签证界面** (2天)
   - 预算编制页面
   - 签证上传页面
   - 双路审核界面

3. **验收与结案界面** (2天)
   - 验收发起页面
   - 现场验收界面（移动端）
   - 结案单查看

4. **报修审核界面** (1天)
   - 副主任审核页面
   - 催办提醒界面

#### 第六阶段: 集成测试与部署（3天）

1. **集成测试** (2天)
   - 完整流程测试
   - 角色权限测试
   - 并发测试
   - 性能测试

2. **部署上线** (1天)
   - 数据库迁移
   - 生产环境部署
   - 监控配置

### 4.2 方案B: 分阶段迭代（推荐快速上线）

**目标**: 先实现核心流程，后续迭代  
**第一阶段工期**: 10-12天  
**第二阶段工期**: 8-10天

#### 第一阶段: MVP（10-12天）
**核心功能**: 立项审批 + 逐级驳回

1. **核心审批流程** (5天)
   - 数据库设计
   - 审批引擎
   - 立项三级审批
   - 逐级驳回

2. **简化预算** (2天)
   - 基础预算功能
   - 简单审核

3. **前端界面** (3天)
   - 立项审批界面
   - 预算界面

4. **测试部署** (2天)

**可上线使用**: 满足核心审批需求

#### 第二阶段: 完善功能（8-10天）

1. **签证管理** (3天)
2. **验收管理** (3天)
3. **报修审核** (1天)
4. **催办通知** (1天)
5. **测试部署** (2天)

### 4.3 方案C: 最小可行（快速验证）

**目标**: 最快速度验证核心概念  
**工期**: 5-7天

1. **简化的两级审批** (2天)
   - 提交人 → 审批人
   - 通过/驳回

2. **基础立项** (2天)
   - 立项创建
   - 简单审批

3. **前端页面** (2天)
4. **测试部署** (1天)

**适用场景**: 概念验证、小规模试用

---

## 📝 五、具体实施建议

### 5.1 推荐方案: 方案B（分阶段迭代）

**理由**:
1. **风险可控**: 分两阶段，每阶段都可验收
2. **快速上线**: 第一阶段10-12天可上线核心功能
3. **持续改进**: 根据实际使用反馈优化第二阶段
4. **成本合理**: 总工期18-22天，可接受

### 5.2 关键技术决策

#### 审批流程引擎
**建议**: 自建简化版

**原因**:
- 需求明确，逻辑不复杂
- 完全可控，易于调整
- 无需学习第三方框架
- 性能更好

**实现方案**:
```typescript
// 审批流程服务
@Injectable()
export class ApprovalService {
  // 创建审批流程
  async createFlow(ticketId, type, steps) { }
  
  // 审批通过
  async approve(flowId, stepNumber, approverId, comment) { }
  
  // 驳回（逐级）
  async reject(flowId, stepNumber, approverId, comment) { }
  
  // 检查是否完成
  async checkComplete(flowId) { }
}
```

#### 双路并行审核
**建议**: 状态机模式

**实现方案**:
```typescript
// 签证状态检查
if (contentStatus === 'APPROVED' && formatStatus === 'APPROVED') {
  certificate.status = 'APPROVED';
  // 推进到下一阶段
}

if (contentStatus === 'REJECTED' || formatStatus === 'REJECTED') {
  certificate.status = 'REJECTED';
  // 通知乙方重新上传
}
```

#### 逐级驳回
**建议**: 递归逻辑

**实现方案**:
```typescript
async rejectToPreviewLevel(flowId, currentStep, comment) {
  // 找到上一级
  const previousStep = await this.findPreviousStep(flowId, currentStep);
  
  // 更新状态
  await this.updateStepStatus(flowId, currentStep, 'REJECTED');
  await this.updateStepStatus(flowId, previousStep, 'PENDING');
  
  // 通知上一级审批人
  await this.notifyApprover(previousStep);
}
```

### 5.3 数据迁移方案

**现有数据处理**:
1. 保留所有历史工单数据
2. 新增字段设置默认值
3. 历史工单标记为"旧系统"
4. 新工单走新流程

**迁移步骤**:
```sql
-- 1. 添加新字段（允许NULL）
ALTER TABLE tickets ADD COLUMN location VARCHAR(200);
ALTER TABLE tickets ADD COLUMN department VARCHAR(100);
ALTER TABLE tickets ADD COLUMN current_phase VARCHAR(20) DEFAULT 'REPAIR';

-- 2. 更新历史数据
UPDATE tickets SET current_phase = 'COMPLETED' WHERE status = 'CLOSED';

-- 3. 创建新表
-- (执行Prisma migrate)
```

### 5.4 角色权限配置

**角色映射方案**:
```typescript
// 旧角色 → 新角色映射
const roleMapping = {
  'USER': 'REPORTER',        // 普通用户 → 报修人
  'SUPPORT': 'VICE_DIRECTOR', // 支持人员 → 副主任
  'ADMIN': 'ADMIN',          // 管理员 → 管理员
};

// 新增角色需要手动分配
const newRoles = [
  'DEPT_MANAGER',    // 部门主管
  'VICE_LEADER',     // 分管领导
  'TOP_LEADER',      // 一把手
  'DATA_ADMIN',      // 资料员
  'INSPECTOR',       // 监察人员
  'FINANCE',         // 财务人员
  'CONTRACTOR',      // 乙方人员
];
```

---

## 📊 六、工作量估算汇总

### 6.1 方案A: 完整实现

| 模块 | 后端 | 前端 | 测试 | 小计 |
|-----|------|------|------|------|
| 审批流程引擎 | 24h | 16h | 8h | 48h |
| 立项管理 | 16h | 12h | 4h | 32h |
| 预算管理 | 16h | 12h | 4h | 32h |
| 签证管理 | 16h | 12h | 4h | 32h |
| 验收管理 | 20h | 16h | 4h | 40h |
| 报修审核 | 8h | 8h | 2h | 18h |
| 催办通知 | 8h | 4h | 2h | 14h |
| 移动端优化 | 0h | 32h | 8h | 40h |
| 集成测试 | - | - | 16h | 16h |
| **总计** | **108h** | **112h** | **52h** | **272h** |

**工期**: 272h / 8h = **34人天** ≈ **7周** (单人)  
**团队**: 2人 ≈ **3.5周** | 3人 ≈ **2.3周**

### 6.2 方案B: 分阶段迭代

#### 第一阶段MVP
| 模块 | 后端 | 前端 | 测试 | 小计 |
|-----|------|------|------|------|
| 审批引擎 | 24h | 16h | 8h | 48h |
| 立项管理 | 16h | 12h | 4h | 32h |
| 简化预算 | 12h | 8h | 4h | 24h |
| 报修审核 | 8h | 8h | 2h | 18h |
| 集成测试 | - | - | 8h | 8h |
| **小计** | **60h** | **44h** | **26h** | **130h** |

**工期**: 130h / 8h = **16人天** ≈ **2周** (单人) | 2人 ≈ **10天**

#### 第二阶段完善
| 模块 | 后端 | 前端 | 测试 | 小计 |
|-----|------|------|------|------|
| 签证管理 | 16h | 12h | 4h | 32h |
| 验收管理 | 20h | 16h | 4h | 40h |
| 催办通知 | 8h | 4h | 2h | 14h |
| 移动端 | 0h | 32h | 8h | 40h |
| 集成测试 | - | - | 8h | 8h |
| **小计** | **44h** | **64h** | **26h** | **134h** |

**工期**: 134h / 8h = **17人天** ≈ **2周** (单人) | 2人 ≈ **10天**

**总工期**: 20-25天（2人团队）

### 6.3 方案C: 最小可行

| 模块 | 后端 | 前端 | 测试 | 小计 |
|-----|------|------|------|------|
| 简化审批 | 16h | 12h | 4h | 32h |
| 基础立项 | 12h | 8h | 4h | 24h |
| 测试部署 | - | - | 8h | 8h |
| **总计** | **28h** | **20h** | **16h** | **64h** |

**工期**: 64h / 8h = **8人天** ≈ **5-7天** (2人)

---

## 💡 七、实施建议

### 7.1 立即行动项

1. **确认方案选择**
   - 评审三个方案
   - 确定时间和预算
   - 选择实施方案

2. **数据库设计评审**
   - 评审Schema设计
   - 确认字段和关系
   - 规划迁移策略

3. **技术预研**
   - 审批引擎设计
   - 逐级驳回逻辑
   - 双路并行机制

4. **团队准备**
   - 分配开发任务
   - 设置开发环境
   - 准备测试数据

### 7.2 风险控制

**主要风险**:
1. **审批逻辑复杂**: 逐级驳回容易出bug
   - 缓解: 充分测试，画状态图

2. **并行审核同步**: 两路审核状态同步复杂
   - 缓解: 使用事务，状态机模式

3. **数据迁移**: 历史数据兼容
   - 缓解: 先在测试环境完整测试

4. **工期延误**: 估算可能不准确
   - 缓解: 采用方案B分阶段，每阶段可验收

### 7.3 成功标准

**第一阶段（MVP）**:
- ✅ 立项三级审批正常运行
- ✅ 逐级驳回逻辑正确
- ✅ 所有角色权限正确
- ✅ 通知推送及时

**第二阶段（完整）**:
- ✅ 签证双路并行审核正常
- ✅ 验收流程完整
- ✅ 结案单自动生成
- ✅ 移动端功能可用

**最终验收**:
- ✅ 100%满足需求文档
- ✅ 通过用户验收测试
- ✅ 性能指标达标
- ✅ 安全审计通过

---

## 📌 八、总结

### 8.1 核心问题

**当前系统 vs 用户需求的本质差距**:
- 当前: 通用工单管理系统（简单状态流转）
- 需求: 审批驱动的流程管理系统（多级审批+逐级驳回）

**完成度**: 约15-20%

**核心缺失**:
1. 多级审批流程引擎 ❌
2. 逐级驳回机制 ❌
3. 双路并行审核 ❌
4. 预算、立项、签证、验收管理 ❌

### 8.2 推荐方案

**方案B: 分阶段迭代** ⭐

**第一阶段（10-12天）**:
- 审批引擎 + 立项管理
- 满足核心审批需求
- 可快速上线验证

**第二阶段（8-10天）**:
- 签证 + 验收 + 移动端
- 完整满足所有需求

**总工期**: 20-25天（2人团队）

### 8.3 下一步行动

1. **立即**: 确认实施方案
2. **本周**: 数据库设计评审
3. **下周**: 启动第一阶段开发
4. **2周后**: 第一阶段验收测试
5. **3-4周后**: 全功能上线

---

**文档版本**: v1.0  
**编制日期**: 2026-09-06  
**建议方案**: 方案B（分阶段迭代）  
**预计工期**: 20-25天（2人团队）

---

✅ 需求理解完整  
✅ 差距分析清晰  
✅ 实施方案可行  
✅ 工作量估算准确  

**准备就绪，等待启动开发！** 🚀
