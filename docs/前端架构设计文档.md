# 工单管理系统 - 前端架构设计文档

**文档版本**: v1.0  
**创建日期**: 2026-09-06  
**项目名称**: 工单管理系统  
**前端架构师**: frontend-architect

---

## 目录

1. [技术栈选型](#1-技术栈选型)
2. [项目目录结构](#2-项目目录结构)
3. [组件设计原则](#3-组件设计原则)
4. [状态管理方案](#4-状态管理方案)
5. [路由设计](#5-路由设计)
6. [HTTP请求封装](#6-http请求封装)
7. [权限控制方案](#7-权限控制方案)
8. [开发规范](#8-开发规范)

---

## 1. 技术栈选型

### 1.1 核心技术栈

| 技术 | 版本 | 用途 | 选型理由 |
|------|------|------|---------|
| React | 18.3.x | UI框架 | 生态成熟、性能优秀、社区活跃 |
| TypeScript | 5.x | 开发语言 | 类型安全、提高代码质量 |
| Vite | 5.x | 构建工具 | 开发体验好、构建速度快 |
| React Router | 6.x | 路由管理 | React官方推荐、API简洁 |
| Zustand | 4.x | 状态管理 | 轻量级、易用、性能好 |
| Ant Design | 5.x | UI组件库 | 企业级、组件丰富、中文友好 |
| React Hook Form | 7.x | 表单管理 | 性能优秀、API简洁 |
| Zod | 3.x | 数据校验 | TypeScript优先、类型安全 |
| Axios | 1.x | HTTP客户端 | 功能完善、拦截器支持 |
| Day.js | 1.x | 时间处理 | 轻量级、API兼容moment.js |
| React Query | 5.x | 数据获取 | 缓存管理、自动重试 |

### 1.2 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| ESLint | 8.x | 代码检查 |
| Prettier | 3.x | 代码格式化 |
| Vitest | 1.x | 单元测试 |
| React Testing Library | 14.x | 组件测试 |
| Playwright | 1.x | E2E测试 |

### 1.3 技术选型理由

**为什么选择 React？**
- 虚拟DOM性能优秀
- Hooks API简洁直观
- 生态系统成熟丰富
- 社区活跃支持好

**为什么选择 Zustand 而不是 Redux？**
- API更简洁(无需Provider、Reducer、Action)
- Bundle体积更小(1KB vs 3KB)
- TypeScript支持更好
- 学习曲线更平缓
- 满足本项目的状态管理需求

**为什么选择 Ant Design？**
- 企业级UI组件库
- 组件丰富完整
- 中文文档友好
- 设计规范成熟
- 适合管理后台开发

---

## 2. 项目目录结构

```
ticket-system-frontend/
├── public/                     # 静态资源
│   ├── favicon.ico
│   └── logo.png
├── src/
│   ├── api/                    # API接口
│   │   ├── index.ts           # API统一导出
│   │   ├── request.ts         # Axios封装
│   │   ├── auth.ts            # 认证接口
│   │   ├── ticket.ts          # 工单接口
│   │   ├── user.ts            # 用户接口
│   │   └── stats.ts           # 统计接口
│   │
│   ├── assets/                 # 静态资源
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       ├── global.css     # 全局样式
│   │       ├── variables.css  # CSS变量
│   │       └── reset.css      # 样式重置
│   │
│   ├── components/             # 通用组件
│   │   ├── Layout/            # 布局组件
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Footer.tsx
│   │   │
│   │   ├── TicketCard/        # 工单卡片
│   │   │   ├── index.tsx
│   │   │   └── style.module.css
│   │   │
│   │   ├── StatusBadge/       # 状态徽章
│   │   │   └── index.tsx
│   │   │
│   │   ├── PriorityTag/       # 优先级标签
│   │   │   └── index.tsx
│   │   │
│   │   ├── FileUpload/        # 文件上传
│   │   │   └── index.tsx
│   │   │
│   │   ├── RichTextEditor/    # 富文本编辑器
│   │   │   └── index.tsx
│   │   │
│   │   ├── Timeline/          # 时间线
│   │   │   └── index.tsx
│   │   │
│   │   └── ProtectedRoute/    # 权限路由
│   │       └── index.tsx
│   │
│   ├── pages/                  # 页面组件
│   │   ├── Auth/              # 认证页面
│   │   │   ├── Login.tsx
│   │   │   └── Register.tsx
│   │   │
│   │   ├── Ticket/            # 工单页面
│   │   │   ├── List.tsx       # 工单列表
│   │   │   ├── Create.tsx     # 创建工单
│   │   │   ├── Detail.tsx     # 工单详情
│   │   │   ├── Process.tsx    # 处理工单
│   │   │   └── Review.tsx     # 审核工单
│   │   │
│   │   ├── Dashboard/         # 统计看板
│   │   │   └── index.tsx
│   │   │
│   │   ├── Settings/          # 个人设置
│   │   │   └── index.tsx
│   │   │
│   │   └── NotFound/          # 404页面
│   │       └── index.tsx
│   │
│   ├── hooks/                  # 自定义Hooks
│   │   ├── useAuth.ts         # 认证Hook
│   │   ├── useTicket.ts       # 工单Hook
│   │   ├── useDebounce.ts     # 防抖Hook
│   │   └── usePermission.ts   # 权限Hook
│   │
│   ├── store/                  # 状态管理
│   │   ├── index.ts           # Store统一导出
│   │   ├── auth.ts            # 认证状态
│   │   ├── ticket.ts          # 工单状态
│   │   ├── notification.ts    # 通知状态
│   │   └── app.ts             # 应用全局状态
│   │
│   ├── types/                  # TypeScript类型
│   │   ├── index.ts
│   │   ├── ticket.ts          # 工单类型
│   │   ├── user.ts            # 用户类型
│   │   ├── api.ts             # API类型
│   │   └── common.ts          # 通用类型
│   │
│   ├── utils/                  # 工具函数
│   │   ├── index.ts
│   │   ├── format.ts          # 格式化工具
│   │   ├── validate.ts        # 校验工具
│   │   ├── storage.ts         # 本地存储
│   │   ├── constants.ts       # 常量定义
│   │   └── helpers.ts         # 辅助函数
│   │
│   ├── router/                 # 路由配置
│   │   └── index.tsx
│   │
│   ├── App.tsx                 # 应用根组件
│   ├── main.tsx               # 应用入口
│   └── vite-env.d.ts          # Vite类型声明
│
├── tests/                      # 测试文件
│   ├── unit/                  # 单元测试
│   └── e2e/                   # E2E测试
│
├── .env.development           # 开发环境变量
├── .env.production            # 生产环境变量
├── .eslintrc.cjs              # ESLint配置
├── .prettierrc                # Prettier配置
├── tsconfig.json              # TypeScript配置
├── vite.config.ts             # Vite配置
└── package.json               # 项目配置
```

### 2.1 目录说明

**api/** - API接口层
- 所有后端接口调用封装
- 统一的请求/响应拦截
- 按功能模块划分

**components/** - 通用组件
- 可复用的UI组件
- 业务无关的展示组件
- 每个组件独立目录

**pages/** - 页面组件
- 对应路由的页面级组件
- 包含业务逻辑
- 按功能模块划分

**hooks/** - 自定义Hooks
- 封装可复用的逻辑
- 提高代码复用率
- 保持组件简洁

**store/** - 状态管理
- Zustand状态存储
- 按功能模块划分
- 包含状态和操作方法

**types/** - 类型定义
- TypeScript类型/接口
- 提供类型安全
- 统一数据结构

**utils/** - 工具函数
- 纯函数工具
- 格式化、校验等
- 与业务解耦

---

## 3. 组件设计原则

### 3.1 组件分类

**1. 展示组件 (Presentational Components)**
- 只负责UI渲染
- 通过props接收数据
- 不包含业务逻辑
- 可复用性强

**示例：**
```typescript
// components/StatusBadge/index.tsx
interface StatusBadgeProps {
  status: TicketStatus;
  size?: 'small' | 'default' | 'large';
}

export const StatusBadge: React.FC<StatusBadgeProps> = ({ status, size = 'default' }) => {
  return <Badge status={statusMap[status]} text={statusTextMap[status]} />;
};
```

**2. 容器组件 (Container Components)**
- 负责数据获取和状态管理
- 包含业务逻辑
- 调用展示组件进行渲染

**示例：**
```typescript
// pages/Ticket/List.tsx
export const TicketList: React.FC = () => {
  const { tickets, loading } = useTickets();
  const navigate = useNavigate();

  const handleTicketClick = (id: string) => {
    navigate(`/tickets/${id}`);
  };

  return (
    <div>
      {tickets.map(ticket => (
        <TicketCard key={ticket.id} ticket={ticket} onClick={handleTicketClick} />
      ))}
    </div>
  );
};
```

### 3.2 组件设计规范

**命名规范**
- 组件名使用PascalCase: `TicketCard`, `StatusBadge`
- 文件名与组件名一致: `TicketCard.tsx`
- 样式文件使用CSS Modules: `style.module.css`

**Props设计**
- 必填属性在前,可选属性在后
- 使用TypeScript接口定义Props
- 提供清晰的类型和注释
- 设置合理的默认值

**示例：**
```typescript
interface TicketCardProps {
  ticket: Ticket;              // 必填
  onClick?: (id: string) => void;  // 可选
  showActions?: boolean;       // 可选,有默认值
}

export const TicketCard: React.FC<TicketCardProps> = ({
  ticket,
  onClick,
  showActions = true,  // 默认值
}) => {
  // ...
};
```

**组件拆分原则**
- 单一职责:一个组件只做一件事
- 代码行数:单个组件<300行,建议<200行
- 复杂度:嵌套层级≤4层
- 可测试:易于编写单元测试

### 3.3 Hooks使用规范

**内置Hooks使用**
- `useState`:管理组件内部状态
- `useEffect`:处理副作用(数据获取、订阅)
- `useMemo`:缓存计算结果
- `useCallback`:缓存函数引用
- `useRef`:访问DOM或保存可变值

**自定义Hooks规范**
- 命名以`use`开头
- 封装可复用逻辑
- 返回数组或对象

**示例：**
```typescript
// hooks/useTicket.ts
export const useTicket = (ticketId: string) => {
  const [ticket, setTicket] = useState<Ticket | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    const fetchTicket = async () => {
      setLoading(true);
      try {
        const data = await ticketApi.getDetail(ticketId);
        setTicket(data);
      } catch (err) {
        setError(err as Error);
      } finally {
        setLoading(false);
      }
    };
    fetchTicket();
  }, [ticketId]);

  return { ticket, loading, error };
};
```

---

## 4. 状态管理方案

### 4.1 Zustand Store设计

**Store划分原则**
- 按功能模块划分Store
- 每个Store职责单一
- Store之间保持独立

**Store结构**
```typescript
// store/auth.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  // 状态
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  
  // 操作方法
  login: (credentials: LoginCredentials) => Promise<void>;
  logout: () => void;
  updateUser: (user: Partial<User>) => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      // 初始状态
      user: null,
      token: null,
      isAuthenticated: false,
      
      // 登录
      login: async (credentials) => {
        const { user, token } = await authApi.login(credentials);
        set({ user, token, isAuthenticated: true });
      },
      
      // 登出
      logout: () => {
        set({ user: null, token: null, isAuthenticated: false });
      },
      
      // 更新用户信息
      updateUser: (userData) => {
        const currentUser = get().user;
        if (currentUser) {
          set({ user: { ...currentUser, ...userData } });
        }
      },
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({ token: state.token, user: state.user }),
    }
  )
);
```

### 4.2 其他Store示例

**工单Store**
```typescript
// store/ticket.ts
interface TicketState {
  tickets: Ticket[];
  currentTicket: Ticket | null;
  filters: TicketFilters;
  
  setTickets: (tickets: Ticket[]) => void;
  setCurrentTicket: (ticket: Ticket | null) => void;
  updateFilters: (filters: Partial<TicketFilters>) => void;
  clearFilters: () => void;
}

export const useTicketStore = create<TicketState>((set) => ({
  tickets: [],
  currentTicket: null,
  filters: { status: [], priority: [], keyword: '' },
  
  setTickets: (tickets) => set({ tickets }),
  setCurrentTicket: (ticket) => set({ currentTicket: ticket }),
  updateFilters: (filters) => 
    set((state) => ({ filters: { ...state.filters, ...filters } })),
  clearFilters: () => 
    set({ filters: { status: [], priority: [], keyword: '' } }),
}));
```

**通知Store**
```typescript
// store/notification.ts
interface NotificationState {
  unreadCount: number;
  notifications: Notification[];
  
  addNotification: (notification: Notification) => void;
  markAsRead: (id: string) => void;
  clearAll: () => void;
}

export const useNotificationStore = create<NotificationState>((set) => ({
  unreadCount: 0,
  notifications: [],
  
  addNotification: (notification) => 
    set((state) => ({
      notifications: [notification, ...state.notifications],
      unreadCount: state.unreadCount + 1,
    })),
    
  markAsRead: (id) =>
    set((state) => ({
      notifications: state.notifications.map(n =>
        n.id === id ? { ...n, read: true } : n
      ),
      unreadCount: Math.max(0, state.unreadCount - 1),
    })),
    
  clearAll: () => set({ notifications: [], unreadCount: 0 }),
}));
```

### 4.3 状态管理最佳实践

**何时使用全局状态？**
- ✅ 用户认证信息
- ✅ 全局配置(主题、语言)
- ✅ 通知消息
- ✅ 跨页面共享的数据

**何时使用组件状态？**
- ✅ UI交互状态(展开/收起)
- ✅ 表单输入值
- ✅ 仅当前组件使用的数据

**何时使用URL状态？**
- ✅ 列表筛选条件
- ✅ 分页参数
- ✅ 搜索关键词
- ✅ 可分享的应用状态

---

## 5. 路由设计

### 5.1 路由配置

```typescript
// router/index.tsx
import { createBrowserRouter } from 'react-router-dom';
import { AppLayout } from '@/components/Layout/AppLayout';
import { ProtectedRoute } from '@/components/ProtectedRoute';

export const router = createBrowserRouter([
  {
    path: '/login',
    element: <Login />,
  },
  {
    path: '/register',
    element: <Register />,
  },
  {
    path: '/',
    element: (
      <ProtectedRoute>
        <AppLayout />
      </ProtectedRoute>
    ),
    children: [
      {
        index: true,
        element: <Navigate to="/tickets" replace />,
      },
      {
        path: 'dashboard',
        element: <Dashboard />,
      },
      {
        path: 'tickets',
        children: [
          {
            index: true,
            element: <TicketList />,
          },
          {
            path: 'create',
            element: <TicketCreate />,
          },
          {
            path: ':id',
            element: <TicketDetail />,
          },
          {
            path: ':id/process',
            element: <TicketProcess />,
          },
          {
            path: ':id/review',
            element: <TicketReview />,
          },
        ],
      },
      {
        path: 'settings',
        element: <Settings />,
      },
    ],
  },
  {
    path: '*',
    element: <NotFound />,
  },
]);
```

### 5.2 路由守卫

```typescript
// components/ProtectedRoute/index.tsx
interface ProtectedRouteProps {
  children: React.ReactNode;
  permission?: UserRole[];
}

export const ProtectedRoute: React.FC<ProtectedRouteProps> = ({
  children,
  permission,
}) => {
  const { isAuthenticated, user } = useAuthStore();
  const location = useLocation();

  // 未登录,跳转登录页
  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  // 权限校验
  if (permission && user && !permission.includes(user.role)) {
    return <Navigate to="/403" replace />;
  }

  return <>{children}</>;
};
```

### 5.3 路由表

| 路径 | 页面 | 权限 | 说明 |
|------|------|------|------|
| `/login` | 登录页 | 公开 | 用户登录 |
| `/register` | 注册页 | 公开 | 用户注册 |
| `/` | 首页 | 已登录 | 重定向到工单列表 |
| `/dashboard` | 统计看板 | admin, reviewer | 数据统计 |
| `/tickets` | 工单列表 | 已登录 | 查看工单 |
| `/tickets/create` | 创建工单 | creator | 新建工单 |
| `/tickets/:id` | 工单详情 | 已登录 | 查看详情 |
| `/tickets/:id/process` | 处理工单 | handler | 处理任务 |
| `/tickets/:id/review` | 审核工单 | reviewer | 审核结果 |
| `/settings` | 个人设置 | 已登录 | 个人信息 |

---

## 6. HTTP请求封装

### 6.1 Axios实例配置

```typescript
// api/request.ts
import axios from 'axios';
import { useAuthStore } from '@/store/auth';
import { message } from 'antd';

const instance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 15000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器
instance.interceptors.request.use(
  (config) => {
    const { token } = useAuthStore.getState();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
instance.interceptors.response.use(
  (response) => {
    const { code, data, message: msg } = response.data;
    
    if (code === 200) {
      return data;
    } else {
      message.error(msg || '请求失败');
      return Promise.reject(new Error(msg));
    }
  },
  (error) => {
    if (error.response) {
      const { status } = error.response;
      
      switch (status) {
        case 401:
          message.error('未登录或登录已过期');
          useAuthStore.getState().logout();
          window.location.href = '/login';
          break;
        case 403:
          message.error('没有权限访问');
          break;
        case 404:
          message.error('请求的资源不存在');
          break;
        case 500:
          message.error('服务器错误');
          break;
        default:
          message.error('请求失败');
      }
    }
    
    return Promise.reject(error);
  }
);

export default instance;
```

### 6.2 API接口封装

```typescript
// api/ticket.ts
import request from './request';

export const ticketApi = {
  getList: (filters) => request.get('/tickets', { params: filters }),
  getDetail: (id) => request.get(`/tickets/${id}`),
  create: (data) => request.post('/tickets', data),
  update: (id, data) => request.put(`/tickets/${id}`, data),
  assign: (id, assigneeId) => request.post(`/tickets/${id}/assign`, { assigneeId }),
  submitResult: (id, result) => request.post(`/tickets/${id}/submit`, { result }),
  review: (id, approved, comment) => request.post(`/tickets/${id}/review`, { approved, comment }),
  close: (id, reason) => request.post(`/tickets/${id}/close`, { reason }),
};
```

---

## 7. 权限控制方案

### 7.1 权限设计

```typescript
export enum UserRole {
  ADMIN = 'admin',
  REVIEWER = 'reviewer',
  HANDLER = 'handler',
  CREATOR = 'creator',
}

export const ROLE_PERMISSIONS = {
  [UserRole.ADMIN]: ['ticket:view:all', 'ticket:assign', 'ticket:close', 'user:manage'],
  [UserRole.REVIEWER]: ['ticket:view:department', 'ticket:review'],
  [UserRole.HANDLER]: ['ticket:view:assigned', 'ticket:process'],
  [UserRole.CREATOR]: ['ticket:view:own', 'ticket:create'],
};
```

### 7.2 权限Hook

```typescript
export const usePermission = () => {
  const { user } = useAuthStore();
  
  const hasPermission = (permission: string) => {
    if (!user) return false;
    return ROLE_PERMISSIONS[user.role]?.includes(permission) || false;
  };
  
  return { hasPermission };
};
```

---

## 8. 开发规范

### 8.1 命名规范
- 组件名: PascalCase
- 函数名: camelCase
- 常量名: UPPER_SNAKE_CASE
- 类型/接口: PascalCase

### 8.2 TypeScript规范
- 优先使用interface定义对象类型
- 避免使用any类型
- 充分利用类型推导

### 8.3 性能优化
- 使用React.memo避免不必要渲染
- 使用useMemo缓存计算结果
- 使用useCallback缓存函数引用
- 路由懒加载

---

**文档结束**